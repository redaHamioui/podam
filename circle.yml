machine:
  java:
    version: oraclejdk8
 

dependencies:
  override:
    #- docker info
    - mvn dependency:resolve -f pom.xml

general:
  artifacts:
    # publish do core
    - $CIRCLE_ARTIFACTS

test:
  override:
    # build
    - mvn install -f pom.xml 

 
