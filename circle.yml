machine:
  java:
    version: oraclejdk8
 

dependencies:
  override:
    #- docker info
    - mvn dependency:resolve -f pom.xml



test:
 
    

  post:
    # testes report
    - mkdir -p junit/
    - find . -type f -regex ".*/target/surefire-reports/.*xml" -exec cp {} junit/ \;
