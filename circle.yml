machine:
  java:
    version: oraclejdk7
 

dependencies:
  override:
    #- docker info
    - mvn dependency:resolve -f podam/pom.xml

general:
  artifacts:
    # publish do core
    - "server/target/modelo.jar"

test:
  override:
    # build
    - mvn install -f podam/pom.xml jacoco:report coveralls:report -DrepoToken=1nqr7w1iwtzAToi58EabiIhjhMgeoZoNR

  post:
    # testes report
    - mkdir -p $CIRCLE_TEST_REPORTS/junit/
    - find . -type f -regex ".*/target/surefire-reports/.*xml" -exec cp {} $CIRCLE_TEST_REPORTS/junit/ \;
